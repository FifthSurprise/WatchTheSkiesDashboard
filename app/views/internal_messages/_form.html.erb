<%= form_for(@internal_message) do |f| %>
  <% if @internal_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internal_message.errors.count, "error") %> prohibited this internal_message from being saved:</h2>

      <ul>
      <% @internal_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label "Message" %> </br>
    <%= f.text_area :message, :size => "60x5"%></br>
    <%= f.label "Author" %> </br>
    <%= f.text_area :author, :size => "60x5"%></br>
    <%= f.submit %>
  </div>
<% end %>
