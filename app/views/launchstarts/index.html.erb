<h1>Listing launchstarts</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @launchstarts.each do |launchstart| %>
      <tr>
        <td><%= link_to 'Show', launchstart %></td>
        <td><%= link_to 'Edit', edit_launchstart_path(launchstart) %></td>
        <td><%= link_to 'Destroy', launchstart, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Launchstart', new_launchstart_path %>
